{
  "name": "alkitab-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "start:backend": "cd backend && npm install && node server.js",
    "start": "npm run build:frontend && npm run start:backend"
  },
  "dependencies": {},
  "devDependencies": {}
}
